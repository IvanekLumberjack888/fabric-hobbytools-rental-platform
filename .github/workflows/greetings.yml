name: Greetings

on: [pull_request_target, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: "Hi there, thanks for opening your first issue in this repository! 🎉 I am really appreciate your contribution and will review it as soon as possible."
        pr-message: "Welcome, and thank you for your first pull request! 🚀 Your contribution helps improve this project. A maintainer will review your changes shortly."
